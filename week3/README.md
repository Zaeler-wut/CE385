3-1 โปรแกรมหาผลรวม โดยเริ่มจากกำหนดตัวแปร เลขคู่  sumEven เลขคี่ productOdd 
และกำหนด i กับ  j สำหรับเป็นตัวบวกและตัวคูณที่จะเพิ่มค่าในลูปทีละ2 
เมื่อเริ่มเข้า while loop  แรกจะทำการบวกค่า i เข้าไปเก็บไว้ใน sumEven และเพิ่มค่า i ไป 2 
จากนั้นแวะมาอีกลูป โดยถ้า J <= 9 จะเอา jที่มีค่าแรกเป็น 3 ไปคูณกับค่าเดิมใน productOdd และเพิ่มค่า j ไป 2
(ทำลูปซ้อนแบบนี้เพราะลูปในจบก่อน) 

3-2  โปรแกรมนี้เป็นโปรแกรมคำนวณค่า BMI โดยเริ่มจากการสร้างฟังก์ชันชื่อ calculateBMI ซึ่งรับค่าพารามิเตอร์คือ weight (น้ำหนัก) และ height        (ส่วนสูง) จากนั้นนำค่าน้ำหนักไปหารด้วย ส่วนสูงคูณส่วนสูง เพื่อเก็บผลลัพธ์ไว้ในตัวแปร bmi เมื่อได้ค่า bmi แล้ว จะใช้คำสั่ง if else เพื่อตรวจสอบช่วงของค่า BMI  เมื่อเรียกใช้ฟังก์ชัน calculateBMI(70, 1.75) โปรแกรมจะนำค่าน้ำหนักและส่วนสูงไปคำนวณค่า BMI  แล้วแสดงผลลัพธ์พร้อมบอกประเภทของร่างกายตามเงื่อนไขที่กำหนดไว้ในฟังก์ชัน